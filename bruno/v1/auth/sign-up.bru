meta {
  name: sign-up
  type: http
  seq: 2
}

post {
  url: {{url}}/v1/auth/sign-up
  body: json
  auth: inherit
}

body:json {
  {
    "email": "superadminx@example.com",
    "password": "password"
  }
}

script:post-response {
  bru.setEnvVar('token',res.body?.meta?.token)
}
